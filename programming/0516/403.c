/* 403 */
#include <stdio.h>
int main(void)
{
  double dt[10];
  dt[0]=2;
  dt[1]=3;
  printf("%f+%f=%f\n", dt[0], dt[1], dt[0]+dt[1]);
 printf("%f-%f=%f\n", dt[0], dt[1], dt[0]-dt[1]);
 printf("%f*%f=%f\n", dt[0], dt[1], dt[0]*dt[1]);
 printf("%f/%f=%f\n", dt[0], dt[1], dt[0]/dt[1]);
 return 0;
  }
