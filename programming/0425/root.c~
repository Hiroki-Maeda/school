/****root.c*********/
#include <stdio.h>
#include <math.h>

int  main(void){

  double   a, answer ;
  printf("Input data >>");
  scanf("%lf",  &a);
  answer=sqrt(a);
  printf("sqrt(%lf)=%f   \n", a, answer);
  return(0);
}
/***********************/
